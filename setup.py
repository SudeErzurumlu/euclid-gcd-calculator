from setuptools import setup, find_packages

setup(
    name="euclid_gcd_calculator",
    version="0.1",
    packages=find_packages(),
    description="A Python package to calculate the GCD using Euclid's algorithm",
    author="Sude",
    license="MIT",
    install_requires=[],
)
